Express is a framework for Node.js which consists of bunch of codes that add extra features, organise and structure your code for web applications built with node.

Nodemon is a NPM package that allows the server to restart automatically once it detects a code change.